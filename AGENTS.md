# AGENT.md
<!-- 最適化方針: 「ターン（レスポンス）数」を最小化する。トークン消費は気にしない。
     1ターンの情報量・作業量を最大化し、往復回数を最小化することが最優先。 -->

## Execution
- 手動コマンド実行は禁止（明示的なデバッグ許可時のみ例外）
---
## Code Style
- インデントはスペース4固定（tab禁止）
- 変更は最小差分で行う（refactor禁止 unless 指示あり）
- 既存の命名規則を維持する
- 不要なformat変更禁止
- コメント追加は必要最小限
- 依存追加は禁止（明示指示時のみ許可）
---
## Information Gathering（ターン数最小化が最優先）
- トークン消費は一切気にしない。read/grepの「回数」より「ターン数」を最小化することを優先する
- 関連しそうなファイルは多少広めでも1ターンでまとめて全部読む（疑わしきは読む）
- 「このファイルも関係あるかも」と思った時点で、確認の往復をせずその場で読む
- 1ターンの中でread/grep/glob等の情報収集ツールは制限なくバッチ実行してよい
- 同一ファイルの再読込は、編集後の確認またはテスト失敗時のみ許可
- 「足りないかもしれないが一旦進める」は禁止。情報収集ターンは妥協せず、その後の修正ターンを安全に1回で終わらせる
- 目安: 修正に入る前の情報収集は1〜2ターン以内に圧縮し、それ以降は確認なしで完結させる
---
## Agent Behavior
- tool callingは可能な限りバッチ化する（1ターンに収まる範囲で最大化する）
- 最低2操作以上まとめて実行する
- 基本方針：「広く読む → 判断 → 修正 → テスト」を可能な限り少ないターン数で完結させる
- 修正前に影響範囲を1ターンでまとめて把握する（複数ファイル・複数箇所を横断的に確認してよい）
- 小さな変更ごとに停止しない
- 推測による質問は基本禁止。ただし下記「確認が必要な場合」のルールに従う
---
## 確認が必要な場合のルール
- 仕様判断や破壊的変更などで本当に確認が必要な場合のみ、質問は1ターンに集約して全て出す
- 質問のための往復は1往復までに圧縮する（多段階の確認は禁止）
- 確認なしで進めた場合は不確実な前提を明示し、後から修正可能な形（小さく分離されたコミット等）にしておく
- 「念のため確認」のための質問は禁止。確認すべきは「進めると引き返しが困難になる判断」のみ
---
## Performance（ターン数最小化基準）
- read/grep回数の節約は一切考慮しない。むしろ迷ったら読む・調べる
- 「同じ検索を繰り返さない」のような制約より「次のターンで再調査が必要にならないこと」を優先する
- 同じエラーログでも、修正後の検証で再取得が必要なら遠慮せず再取得する
- 判断材料が薄いまま推論で押し切ることは禁止。推論より実際の確認を優先する
- 唯一最小化すべき指標は「タスク完了までのレスポンス（ターン）数」
---
## Security Workflows
- 脆弱性指摘は必ず以下を含める
    - 攻撃シナリオ
    - 再現条件
    - 修正案
- speculative warningは禁止
- 根拠のない危険性指摘は禁止
- 実際のコード上の証拠を示す（証拠集めのための追加読み込みは惜しまない）
- 修正時は既存セキュリティ機構を壊さない
- 複数の脆弱性候補がある場合は、個別に確認往復をせず1ターンでまとめて調査・検証する
---
## Large Tasks
- 関連する修正はまとめて実施する
- 小さな修正ごとの報告は禁止
- 複数の脆弱性は可能な限り同時に調査・同時修正する
- 複数ファイルの変更は1回でまとめる
- 最終報告のみ行う（中間ターンでの部分報告は禁止）
---
## Output Rules
- 結論を先に書く
- 変更内容を箇条書きで整理する
- テスト結果を最後に記載する
- 不要な説明は禁止（説明の短さよりターン数の少なさを優先するため、必要なら説明は長くてよい。省略して後で聞き直しが発生する方が悪い）
- 不確実な内容は明示する
---
## Forbidden
- 小さな変更ごとに停止する
- 推測だけで脆弱性を指摘する
- 指示のないリファクタリング
- 不要な依存追加
- 不要なコード整形
- タスク途中で確認を要求する（「確認が必要な場合のルール」に該当するケースを除く）
- 情報不足を承知のまま推論だけで修正を進める
- 完了に必要な調査を「ターン節約」を理由に省略する
---
## Rate Limit Optimization（最重要原則）
- 唯一のボトルネックはRPM（リクエスト回数）。トークン量は最適化対象から完全に除外する
- 可能な限り1レスポンスで「調査+修正+検証準備」までまとめて行う
- 中間報告は禁止
- 完了報告のみ行う
- 追加調査のための単独ターンは禁止 → 追加調査が必要になりそうな箇所は、最初の調査ターンで先回りして全部読む
- 判断に迷ったときの基準：「これを今読まずに進めて、後で読み直しのターンが発生する可能性があるか？」→ Yesなら今読む
